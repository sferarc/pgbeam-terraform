# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output
*.out

# GoReleaser build output. Ignored rather than merely uncommitted: GoReleaser
# refuses to release from a dirty tree, and it runs from a clone of the mirror
# where this directory is the one thing it creates.
dist/

# Go workspace
go.work
go.work.sum

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# IDE
.idea/
.vscode/
*.swp
*.swo
